#!/bin/bash
# This script is a wrapper for the actual scripts that can be run in the shell.

source ~/.wrapper_scripts/functions/colours_func.bash
source ~/.wrapper_scripts/functions/menu_func.bash

overview_menu